generator client {
  provider = "prisma-client.js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model User {
  id              string       @id  @default(uuid())
  username        string       @unique
  email           string       @unique
  password        string       
  created_at      
}